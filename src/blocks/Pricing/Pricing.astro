---
import PricingCard from "../../components/Pricing/PricingCard.astro";
import Container from "../../components/Container/Container.astro";

interface Plan {
  name: string;
  price: string;
  period?: string;
  description?: string;
  features: string[];
  ctaLabel: string;
  ctaHref: string;
  featured?: boolean;
}

interface Props {
  id?: string;
  eyebrow?: string;
  title?: string;
  description?: string;
  plans: Plan[];
  class?: string;
}

const { id, eyebrow, title, description, plans, class: className } = Astro.props;
---

<Container as="section" id={id} class:list={["py-20", className]}>
  {
    (eyebrow || title || description) && (
      <div class="mx-auto mb-14 max-w-2xl text-center">
        {eyebrow && (
          <span class="inline-block border-2 border-ink-on-accent bg-blue px-3 py-1 font-mono text-xs font-bold text-ink-on-accent uppercase tracking-wide shadow-brutal-sm">
            {eyebrow}
          </span>
        )}
        {title && <h2 class="mt-4 text-balance font-display text-section-lg font-extrabold uppercase">{title}</h2>}
        {description && <p class="mt-4 text-pretty text-ink/70">{description}</p>}
      </div>
    )
  }

  <div class="grid gap-8 lg:grid-cols-3">
    {plans.map((plan) => <PricingCard {...plan} />)}
  </div>
</Container>
