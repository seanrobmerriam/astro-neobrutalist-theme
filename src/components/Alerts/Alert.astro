---
type Variant = "info" | "success" | "warning" | "error";

interface Props {
  variant?: Variant;
  message: string;
  class?: string;
}

const { variant = "info", message, class: className } = Astro.props;

const accents: Record<Variant, string> = {
  info: "bg-blue",
  success: "bg-green",
  warning: "bg-orange",
  error: "bg-pink",
};
---

<div role="alert" class:list={["flex items-start gap-3 border-2 border-ink p-4 text-ink shadow-brutal", accents[variant], className]}>
  <svg aria-hidden="true" viewBox="0 0 16 16" fill="currentColor" class="mt-0.5 size-4 shrink-0">
    <path
      fill-rule="evenodd"
      d="M15 8A7 7 0 1 1 1 8a7 7 0 0 1 14 0ZM9 5a1 1 0 1 1-2 0 1 1 0 0 1 2 0ZM6.75 8a.75.75 0 0 0 0 1.5h.75v1.75a.75.75 0 0 0 1.5 0v-2.5A.75.75 0 0 0 8.25 8h-1.5Z"
      clip-rule="evenodd"
    />
  </svg>

  <strong class="block flex-1 leading-tight font-semibold">
    <span class="sr-only">{variant}: </span>
    {message}
  </strong>
</div>
